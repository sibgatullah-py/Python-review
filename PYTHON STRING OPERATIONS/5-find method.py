# find() method is search for the specific letter in the string
str = "this is a dummy text"
print(str.find('text'))
# The answer will be 16 as the text word is found on the 16 index 
# we can also put starting and ending value like range() function .find(value, start, end)