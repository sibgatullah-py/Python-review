# rstrip() method is used to remove a specific character from the right of the string
str = "bangladesh#####"
print(str.rstrip('#')) # if we don't define which character needs to be removed it removes space as default