# index() method is used to find the first occurrence of a specific value

str = 'This is a demo data'
print(str.index('a'))
# we can also put starting and ending value like range() function .find(value, start, end)